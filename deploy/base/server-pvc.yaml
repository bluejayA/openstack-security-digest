apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: server-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # storageClassName omitted → uses the cluster default (local-path on OrbStack).
